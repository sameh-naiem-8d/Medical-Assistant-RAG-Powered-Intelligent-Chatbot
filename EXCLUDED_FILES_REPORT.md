# Excluded Files Report

The GitHub-ready repository excludes `.env`, secrets, VCS metadata, virtual environments, caches, runtime logs, browser profiles, and large runtime artifacts from normal Git tracking.

Large artifacts are described in `artifacts_manifest.json` and are copied locally by setup scripts. Secret values were not copied.
