# Runtime Artifacts

Large runtime artifacts are intentionally not committed as normal Git objects. Run `python scripts/setup_from_local_archive.py --archive "D:/Project Graduation/MEDBRIDGE_FINAL_AI_DELIVERY"` to copy them from the verified local archive, or configure URLs in `artifacts_manifest.json` and run `scripts/download_artifacts.py` later.
